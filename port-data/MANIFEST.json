{
  "release": "v0.14.2",
  "engine": "gnuin-compose-core",
  "engine_source": "gnu.in-os/engine/gnuin-compose-core (authoritative; port-data mirrors it — see PARITY.md)",
  "renderer": "tiny-skia · SoftwareRenderer",
  "envelope": "protocol::Envelope v1",
  "generated_by": "Central (gnu.in-OS simulator)",
  "surfaces": [
    {
      "kind": "background",
      "band": 0,
      "interactive": false,
      "status": "complete",
      "proof": "render"
    },
    {
      "kind": "wallpaper",
      "band": 1,
      "interactive": false,
      "status": "partial",
      "proof": "render · backdrop P6"
    },
    {
      "kind": "bar",
      "band": 30,
      "interactive": true,
      "status": "complete",
      "proof": "render+layer-record"
    },
    {
      "kind": "dock",
      "band": 30,
      "interactive": true,
      "status": "complete",
      "proof": "render+layer-record"
    },
    {
      "kind": "sidebar",
      "band": 60,
      "interactive": true,
      "status": "complete",
      "proof": "cutover P7-tail"
    },
    {
      "kind": "context-menu",
      "band": 100,
      "interactive": true,
      "status": "complete",
      "proof": "M6 render+layer-record"
    },
    {
      "kind": "launcher",
      "band": 120,
      "interactive": true,
      "status": "complete",
      "proof": "cutover P7-tail"
    },
    {
      "kind": "modal",
      "band": 500,
      "interactive": true,
      "status": "pending",
      "proof": "spec-only"
    },
    {
      "kind": "osd",
      "band": 800,
      "interactive": false,
      "status": "complete",
      "proof": "cutover P7"
    },
    {
      "kind": "notification",
      "band": 850,
      "interactive": false,
      "status": "complete",
      "proof": "cutover P7"
    },
    {
      "kind": "screen-corner",
      "band": 880,
      "interactive": false,
      "status": "pending",
      "proof": "overlay passthrough"
    }
  ],
  "complete": 8,
  "pending": 2,
  "fixtures": [
    "scenes/scene.idle.json",
    "scenes/scene.launcher.json",
    "scenes/scene.sidebar.json",
    "scenes/scene.osd.json",
    "scenes/scene.modal.json",
    "scenes/scene.notification.json",
    "scenes/scene.context-menu.json",
    "scenes/scene.full.json",
    "scenes/scene.launcher.motion.json",
    "scenes/taskbar-plan.json",
    "scenes/dock-affordance.json",
    "scenes/menu-vertical.json",
    "scenes/input-focus.json",
    "scenes/edge-aware.json",
    "scenes/diff-damage.json",
    "scenes/render-frame.json",
    "scenes/wire-protocol.json",
    "scenes/compositor-profile.json",
    "scenes/panel-family.json",
    "scenes/settings-pages.json",
    "scenes/keyboard-nav.json"
  ],
  "files": {
    "types": "scene_types.rs",
    "tokens": "tokens.json",
    "motion": "motion.spec.json",
    "taskbar": "taskbar_registry.rs",
    "bar_settings": "bar_settings.rs",
    "compositor_profile": "compositor_profile.rs",
    "panel_family": "panel_family.rs",
    "settings_pages": "settings_pages.rs",
    "keyboard_nav": "keyboard_nav.rs",
    "dock_affordance": "dock_affordance.rs",
    "dock_settings": "dock_settings.rs",
    "dock_items": "dock_items.rs",
    "launcher": "launcher.rs",
    "quick_settings": "quick_settings.rs",
    "media_player": "media_player.rs",
    "resources": "resources.rs",
    "authority": "authority.rs",
    "theme": "theme.rs",
    "material": "material.rs",
    "shell_settings": "shell_settings.json",
    "compose": "compose_core.rs",
    "motion_core": "motion_core.rs",
    "render_core": "render_core.rs",
    "host_motion_driver": "host_motion_driver.rs",
    "host_menu_state": "host_menu_state.rs",
    "host_protocol": "host_protocol.rs",
    "molecules": "molecule_specs.json",
    "parity": "PARITY.md",
    "manifest": "MANIFEST.json"
  },
  "host": {
    "source": "gnu.in-os/engine/gnuin-compose-host (sctk/tiny-skia, Branch-H gnuin-bar pattern)",
    "files": { "motion_driver": "host_motion_driver.rs", "menu_state": "host_menu_state.rs", "protocol": "host_protocol.rs" },
    "motion_driver": "per-surface frame-tick adoption of the engine motion: MotionDriver{ open/dismiss (retarget via interrupt_motion), tick(dt), sample(), is_animating() } — exactly Central's menu-lifecycle wiring; 5 tests",
    "menu_state": "pure overlay lifecycle SM enforcing AT-MOST-ONE surface: open(replace)/configure(promote;no-op-if-dismissed)/dismiss(zero-all)/on_hit(MenuRow->Action,Panel/Blob->KeepOpen,None->Dismiss)/surface_action(Reuse same-output|Recreate) — 8 tests",
    "protocol": "newline-delimited JSON over a Unix socket (QML ContextMenuService ↔ Rust host): HostMessage{Open{request,screen}|Close} · Envelope{version,#[flatten] message} (version-negotiated, is_supported) · HostEvent{Action{id}}; canonical wire frames in scenes/wire-protocol.json (TRANSITIONAL — QML-cutover bridge; the destination is in-process: the host links compose-core directly, no QML / no socket)",
    "ties_to_central": "MotionDriver == cmMotion + advanceCmMotion + openK-interrupt; MenuState.on_hit == Central input-model click intents; surface_action == per-output Reuse/Recreate"
  },
  "compose_engine": {
    "spec": "COMPOSITION_ENGINE_SPEC.md (menus first)",
    "authoritative_crate": "gnu.in-os/engine/gnuin-compose-core v0.14.2 (std-only, optional serde)",
    "port_data_role": "compose_core.rs is a conformance MIRROR of the crate; scenes/menu-vertical.json is real-serde Scene JSON (Scene::from_json-loadable). See PARITY.md.",
    "thesis": "one engine owns all chrome nodes, resolves tokens, composes in a single ordered pass — no node reads a peer's visibility",
    "model": "Scene { screen, nodes:[Node{ id, kind, rect, z, state, tokens, payload }] }",
    "node_kinds": ["Background", "Wallpaper", "Bar", "Dock", "Sidebar", "Blob", "MenuPanel", "MenuRow", "Osd", "Notification", "Launcher", "Overlay", "Modal", "ScreenCorner"],
    "node_ids": ["Bar", "Dock", "Background", "Wallpaper", "MenuPanel", "MenuSub(u8)", "MenuRow{group,row}", "Blob(Backed{Menu|Bar|Dock})"],
    "node_states": ["Hidden", "Opening", "Open", "Committing", "Closing"],
    "payloads": ["None", "Menu(MenuModel)", "Blob(BlobBacking)", "MenuRow(MenuRowData)", "Wallpaper(WallpaperData)"],
    "layer_bands": { "background": 0, "wallpaper": 50, "blob": 100, "dock": 150, "bar": 200, "sidebar": 250, "menu": 300, "menu_row": 305, "menu_sub": 310, "osd": 350, "notification": 380, "launcher": 390, "overlay": 400, "modal": 420, "screen_corner": 450 },
    "menu_styles": { "chrome_backed": ["list", "brand", "trayaudio", "traynetwork", "traybluetooth", "traypower", "trayunified"], "bespoke_self": ["bubble", "bubbletree", "radial", "radialpill", "nestedradialpill", "radialdetached", "concentric", "megapanel", "fractal", "drill", "cascade", "windowtilegrid", "widgetcard", "workspacecard", "widgetpills", "windowcompact"] },
    "blob": "managed node behind chrome menus/bar/dock; rect = element.inflate(DEFAULT_EDGE_MARGIN=6); PerfTier{Sdf->extruded, Static->rounded-rect} picks the rasterizer, never toggles the node; payload Blob{ backs }",
    "reduce": "reduce(ComposeInput)->Scene — single pass: dock(+blob), exactly-one bar(+blob), menu(blob FIRST chrome-only -> MenuPanel -> one MenuRow per hittable row)",
    "diff": "diff(prev,next)->SceneDiff{changes:[NodeChange{Added|Removed|Moved|Updated}]}; damage(prev,next)->Rect",
    "input_region": "live interactive rects; Blob/Background/Wallpaper/ScreenCorner excluded (NodeKind::is_interactive)",
    "retires": ["#4 panneau-noir geometry handshake", "#5/#3 bar double-surface / membraneOwnsBar", "usesMembraneChrome per-style cross-surface opt-in"],
    "proof": "exactly 1 Bar + 1 MenuPanel & <=1 menu Blob across all styles · blob rect = panel.inflate(6) · blob excluded from input · hit at row centre -> MenuRow · in-crate unit tests + compose_core.rs parity_tests",
    "input_model": {
      "rapport": "(cursor_pos + cursor_focus_data_node) × {click-awareness, click-away, click-select, click-unfocus}",
      "types": "CursorState{ x, y, over:Option<NodeId>, over_kind, over_z } · ClickIntent{ Awareness{over} | Away{surface} | Select{node} | Unfocus } · Transient{ Menu, Launcher, Sidebar }",
      "track": "CursorState::track(scene,x,y) — Scene::hit every move; topmost INTERACTIVE node (blobs/backings excluded)",
      "resolve": "resolve_click(scene,x,y,open,has_focus)->ClickIntent — pure+total; precedence away > select > unfocus > awareness",
      "selectable": ["MenuRow", "Bar", "Dock", "Launcher"],
      "fixture": "scenes/input-focus.json (6 cases)",
      "proof": "exactly one intent per press · blob skirt never absorbs · engine owns focus + dismissal (no peer visibility toggles) · 6 input_tests in compose_core.rs"
    },
    "edge_aware": {
      "problem": "reproducible across ALL surfaces — a deeper issue, not a per-surface offset: child affordances + hit/focus rects were placed in absolute space and never intersected against the parent surface's content box or the work-area, so they bleed past borders near any edge (the 'New' row outline + submenu bridge ran straight past the panel's right border).",
      "fix": "one geometry pass: Rect::intersect clips children to their surface; Rect::clamp_inside shifts (not shrinks) transient surfaces into the work-area; submenu_rect flushes to the panel edge and FLIPS to the opposite side at the screen edge. Hit-region and painted rect share one value.",
      "api": "Rect::intersect(parent) · Rect::clamp_inside(area, margin) · submenu_rect(panel, area, size, margin)->(Rect, Side{Left|Right})",
      "fixture": "scenes/edge-aware.json",
      "proof": "row ring never exceeds panel · submenu flips left at the right edge · panel shifted not shrunk · 6 edge_tests in compose_core.rs"
    },
    "diff_damage": {
      "thesis": "retained-mode reactivity — state → reduce → new Scene → diff → targeted host updates; damage() gives the dirty region so a motion-only restyle never forces a full-screen repaint (E5).",
      "types": "NodeChange{ Added|Removed|Moved|Updated }(NodeId) · SceneDiff{ changes }",
      "diff": "diff(prev,next)->SceneDiff — Removed first, then per-next Added | (Moved on rect change) + (Updated on state/z/tokens/payload); a move+restyle reports BOTH",
      "damage": "damage(prev,next)->Rect — union of changed rects (next rect; + prev rect when moved/removed); Rect::ZERO when nothing changed",
      "fixture": "scenes/diff-damage.json",
      "proof": "identical=empty/zero · move≠add/remove · restyle-in-place=Updated not Moved · move+restyle=both · motion-only damage < screen · moved damage unions old+new · 10 diff_tests in compose_core.rs"
    },
    "motion": {
      "mirror": "motion_core.rs (port of src/motion.rs) — the non-rendering half of the motion engine",
      "types": "Easing{ Linear|EaseOutCubic|EaseInOutCubic|Spring(Spring) } · Spring{ response_ms, damping_ratio } (soft=320/0.82) · MotionSpec{ duration_ms, easing, reduced_motion } (default ease_out 240) · MotionSample{ state, raw_progress, curve_progress, visible_progress }",
      "advance": "advance_motion(state, elapsed_ms, spec) — Opening→Open, Closing→Hidden; spring curve may overshoot >1.0 while visible_progress stays input/opacity-safe 0..1",
      "interrupt": "interrupt_motion(current, target, spec) — retarget in-flight WITHOUT a visible jump (monotonic easings preserve visible_progress via inverse_sample)",
      "reduced_motion": "effective_duration_ms()=0 → snap to terminal state",
      "proof": "6 motion tests (overshoot-safe, reduced-snap, closing-inverse, both interruption directions)"
    },
    "render": {
      "mirror": "render_core.rs (port of src/render.rs) — the renderer abstraction (E6/M5)",
      "thesis": "a host holds a Renderer and never embeds a rasterizer; the renderer is a PURE CONSUMER of the scene model (paints what reduce produced, in z-order, from resolved tokens). Swapping tiny-skia ↔ wgpu/vello changes nothing about Scene/ids/tokens/input/motion.",
      "types": "RenderFrame{ scene, diff, damage, motion } · trait Renderer{ type Target; render(&mut self, frame, target) }",
      "illustrative_backend": "RecordingRenderer → DrawList[DrawCmd::RoundedRect] — host-side pattern (real backends bring tiny-skia/wgpu); emits one primitive per live node in back-to-front z-order",
      "fixture": "scenes/render-frame.json",
      "proof": "paints live nodes only · back-to-front z (100<200<300<305) · blob below the chrome it backs · scene unchanged after render · 3 render tests"
    },
    "menu_lifecycle_wired": {
      "note": "Central's context menu is driven by the motion engine (advance_motion) — cm.p IS visible_progress; openK retargets in-flight via interrupt_motion so closing mid-open reverses without a jump (easeOut 220ms). Other surfaces keep the physical spring solver (velocity-carrying, also interruption-smooth)."
    }
  },
  "theming": {
    "owner": "foundational — Theme selects a Palette; SurfaceRole materials resolve from it",
    "files": ["theme.rs", "material.rs"],
    "theme": "Theme { Dark, Light } -> Palette { bg_base, bg_raised, bg_inset, text, subtext, line, scrim, on_accent }",
    "material": "SurfaceRole { Chrome, Surface, Inset, Scrim }.resolve(theme) -> Material { fill, border, text, elevation }; follows_theme() — Chrome is a persistent dark material in both themes, Surface/Inset/Scrim flip",
    "wired": "theme toggle in General CC + a Materials legend; applied this pass to the DESKTOP wallpaper and the LAUNCHER surface (full dark<->light). Chrome (bar/dock) + sidebar + CC inspector stay dark by design; broader rollout incremental",
    "proof": "theme toggle round-trips · palettes differ on every role · on_accent stable · chrome theme-invariant · surface follows + most elevated · inset from palette · 7 tests"
  },
  "context_menu_delegation": {
    "owner": "reuses authority.rs",
    "wired": "context-menu items with risk=system no longer run under ambient authority — they route through requestAuthorizedAction(System) -> the same polkit-style authorization modal (remember 5-min grant). Non-system items unchanged. e.g. 'Full Burn', 'redémarrer le compositeur'"
  },
  "control_center": {
    "owner": "GnuSettingsApp + ShellSettingsService (Quick page projects live session state)",
    "file": "quick_settings.rs",
    "model": "QuickToggle { Wifi, Bluetooth, Dnd, NightShift } · clamp_brightness + brightness_dim (live backlight overlay) · volume_icon tiers · PowerAction { Lock, Logout, Restart, Shutdown }.needs_confirm()",
    "page": "new first CC section 'Quick' — now-playing media card · connectivity tiles · volume + brightness sliders (brightness dims a real screen overlay) · workspace switcher · session/power row with confirm dialog",
    "media": {
      "file": "media_player.rs",
      "model": "MediaPlayer { tracks, index, playing, pos_s } · advance(dt) auto-wraps · toggle/next/prev (prev restarts within 3s else steps) · seek_fraction clamp · progress + fmt_time",
      "wire": "advanced from the RAF step(dt); ONE model feeds BOTH the Quick now-playing card AND the bar media widget — the latter is now a live chip (equalizer glyph when playing + track title + play/pause toggle; click opens Quick), enabled by default",
      "proof": "advance auto-wraps · paused holds · prev restart-then-step · prev wraps to last · seek clamps · m:ss formatting · 6 tests"
    },
    "resources_monitor": {
      "file": "resources.rs",
      "surface": "bar `resources` widget + EXPERT system-monitor popover",
      "model": "RingBuffer { data, cap } (push evicts oldest · last/peak/avg · bars(n) sparkline) · walk_step (bounded EWMA walk) · cpu_target(open_windows, settings_open)",
      "wire": "sampled ~5 Hz from the RAF step(dt); CPU/RAM correlated to live window load. Bar chip = live CPU sparkline + CPU·RAM readout; click opens a popover with CPU/RAM/Network sparklines + current/peak. Enabled by default",
      "proof": "ring caps + drops oldest · bars floor 6 + clamp 100 · bars last-n · walk toward target + clamps · cpu_target tracks load · 5 tests"
    },
    "nav_search": "CC nav has a fuzzy section filter reusing gnuin-launcher::fuzzy_match (matched glyphs highlighted)",
    "services": {
      "file": "authority.rs",
      "principle": "PROPER DELEGATION OF AUTHORITY — every service has exactly ONE owning authority; a privileged action never runs under ambient authority, it is delegated to the owner via an explicit polkit-style authorization step (optionally remembered for a 5-min TTL)",
      "model": "Authority { Session(ambient), System, Compositor } · Service { owner, risk, status } · authorize(svc, grants, now) -> Granted | NeedsDelegation(authority) · GrantTable { holds, grant, revoke, ttl_secs } · status_after",
      "page": "Services CC section pulled out of the generic app-page renderer into a bespoke authority manager: 3-authority legend (owned count + live delegated-grant countdown + revoke) · service registry (risk badge · authority badge · status · start/stop/restart) · authorization prompt modal (service/action/risk/authority + remember toggle + grant/deny)",
      "risk_vocab": "reuses the context-menu observe/local/system blast-radius vocabulary",
      "proof": "session granted w/o delegation · privileged needs delegation · remembered grant passes until expiry · revoke drops immediately · ttl counts down (session=0) · status_after · 6 tests"
    },
    "proof": "brightness clamps 10..100 · dim zero at full + rises as brightness drops · volume glyph tiers · toggle states per kind · every power action confirms + labelled · 5 tests"
  },
  "launcher": {
    "owner": "gnuin-launcher (algorithmic core) + host calculator",
    "file": "launcher.rs",
    "model": "fuzzy_match(query, target) -> Option<FuzzyMatch { score, positions }> · Provider { Apps, Commands, Calc } + route(raw) · looks_like_math(body) · FrecencyEntry { count, last_ms }.score(now)",
    "providers": {
      "apps": "fuzzy over name + keyword aliases; empty query -> frecency-ranked recents",
      "commands": "> prefix · shell command registry wired to real Central actions (Control Center, sidebar, terminal, night-shift, reduce-motion, DND, close-all, capture)",
      "calc": "= prefix or auto-detected arithmetic -> guarded host evaluator; Enter copies result"
    },
    "interaction": "↑↓ select · ↵ run active · Tab cycles provider · Esc closes; matched glyphs bolded from FuzzyMatch.positions",
    "proof": "non-subsequence rejected · positions correct · consecutive beats scattered · prefix + word-boundary bonuses · routing strips/trims · math autodetect · frecency recent+frequent ranks higher · 10 tests"
  },
  "dock": {
    "note": "the dock has three real, orthogonal backend layers — visibility · layout · live app-state — none subsumes another; APPEARANCE personalization (DockAppearance) resolves the ShellPage dock pending_parity (now fully closed)",
    "visibility_layer": {
      "owner": "ShellSettingsService + DockService (ShellIpc dock.*)",
      "file": "dock_settings.rs",
      "model": "DockSettings { enabled, archived, mode, reveal_delay_ms, hide_delay_ms, pinned }",
      "modes": ["docked", "floating", "autohide", "dodge"],
      "resolve": "resolve(DockContext{ window_overlaps, edge_hovered, pointer_in_dock }) -> DockResolution{ painted, visible, reserves_zone, reveal_strip }",
      "proof": "docked reserves / floating does not · autohide hidden until edge-hover · dodge hides only under overlap · pinned always visible+reserves · 6 tests"
    },
    "layout_layer": {
      "owner": "gnuin-dock::affordance (plan gnu.in.dock-affordance-plan.v1, planned by asset-core)",
      "file": "dock_affordance.rs",
      "model": "DockAffordanceConfig { deck_style: Row|Stack|Fan, card_px, fan_arc_deg, spread, items }"
    },
    "items_layer": {
      "owner": "foreign-toplevel list + NotificationService, joined against ShellSettingsService.dockPinned",
      "file": "dock_items.rs",
      "model": "DockItems { pinned } + DockItem { app, pinned, windows, badge, attention } + Magnify { enabled, strength, reach }",
      "resolve": "slots(running, state) -> [Slot::App | Slot::Separator] (pinned, then transient running apps after a separator); Magnify::scale_at(index, hovered) -> per-slot Row magnification (falloff over slot distance)",
      "indicators": ["running", "window-count dots (cap 3)", "notification badge (9+)", "attention bounce", "pinned vs transient"],
      "proof": "transient apps follow a separator · no separator without transient · badge saturates 9+ · dots cap 3 · magnify peaks at cursor and decays to rest · disabled/no-hover is flat · window previewable + WindowPreview · 7 tests"
    },
    "appearance": {
      "owner": "Central personalization, resolving ShellPage.qml dock pending_parity",
      "file": "dock_settings.rs (DockAppearance)",
      "model": "DockAppearance { indicator: Dot|Line|None, labels: Hover|Always|Never, separators, margin_px, opacity, radius_px, elevation: Flat|Lifted|Floating }",
      "resolves_pending_parity": ["grouped app indicators", "persistent pin/unpin", "separators / shadows / elevation margins", "window preview popups"],
      "remaining": [],
      "proof": "card radius tracks tray + floors · dot/line/none indicator · elevation tokens grow · tray rgba uses opacity · 4 tests"
    }
  },
  "dock_affordance": {
    "deck_styles": ["Row", "Stack", "Fan"],
    "config": "DockAffordanceConfig { deck_style, card_px, fan_arc_deg, spread, items }",
    "mounted_from": "asset-core://manifest",
    "proof": "one config -> N CardPlacement via place(); no per-style host path (deck of cards without rewriting the runtime)"
  },
  "taskbar_registry": {
    "handoff_key": "shell.taskbar.modules_json",
    "open_registry": true,
    "builtins": 9,
    "ext_modules": 3,
    "replaces": "closed enum TaskbarWidgetId (gnuin-bar/src/settings.rs:307 parse_widget_id)",
    "unknown_id": "resolves to None -> surfaced diagnostic, never silently dropped",
    "slots": ["left", "center", "right"],
    "per_output": ["edge", "visible", "density"]
  },
  "motion_spec": {
    "version": "0.4.0",
    "imported_from": "GNU.IN Design System (legacy) motion-spec ui_kit",
    "atoms": 15,
    "molecules": 12,
    "mascot_states": 5,
    "eases": 8,
    "durations": 9,
    "consumes": "scene_motion.rs maps eases→gpui bezier closures, engine_springs→compose-core solver"
  }
}